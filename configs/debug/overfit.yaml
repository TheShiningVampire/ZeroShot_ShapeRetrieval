# @package _global_

# overfits to 3 batches

defaults:
  - default.yaml

trainer:
  max_epochs: 35
  overfit_batches: 1
  accelerator: gpu

# model ckpt and early stopping need to be disabled during overfitting
callbacks: null
